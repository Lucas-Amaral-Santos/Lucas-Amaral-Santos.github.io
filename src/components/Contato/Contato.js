import React from "react";
import styles from "../../App.module.css";

class Contato extends React.Component {
    render () {
        return (
            <div id="contato" className={styles.whiteStyle}>
                <h1>Contato</h1>

            </div>
    )};
}

export default Contato;